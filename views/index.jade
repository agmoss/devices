extends layout

block content
  h1= title
  p Welcome to #{title}

  select#selectStatus

  // D3 SVG
  #viz
    
  script.
    var data1 = JSON.parse('!{JSON.stringify(json)}');
    var data = timeSeries(data1);